/** Preferred public origin — matches live apex→www redirect. */
export const SITE_HOST = "www.ripsportal.com";
export const SITE_URL = `https://${SITE_HOST}`;
/** Apex host (redirect target source). */
export const SITE_APEX_HOST = "ripsportal.com";
